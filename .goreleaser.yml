before:
  hooks:
    - go mod download
builds:
- env:
    - CGO_ENABLED=0
  main: ./cmd/templatizer
  goos:
    - darwin
    - linux
    - windows
  goarch:
    - amd64
archives:
- format_overrides:
    - goos: windows
      format: zip
release:
  github:
  prerelease: auto
dockers:
  -
    image_templates:
    - "docker.pkg.github.com/wesovilabs/tempaltizer/{{ .ProjectName }}:latest"
    - "docker.pkg.github.com/wesovilabs/tempaltizer/{{ .ProjectName }}:{{ .Major }}"
    - "docker.pkg.github.com/wesovilabs/tempaltizer/{{ .ProjectName }}:{{ .Major }}.{{ .Minor }}"
    - "docker.pkg.github.com/wesovilabs/tempaltizer/{{ .ProjectName }}:{{ .Major }}.{{ .Minor }}.{{ .Patch }}"
    build_flag_templates:
    - "--pull"
    - "--label=org.wesovilabs.image.created={{.Date}}"
    - "--label=org.wesovilabs.image.name={{.ProjectName}}"
    - "--label=org.wesovilabs.image.revision={{.FullCommit}}"
    - "--label=org.wesovilabs.image.version={{.Version}}"
    - "--label=org.wesovilabs.image.source={{.GitURL}}"
